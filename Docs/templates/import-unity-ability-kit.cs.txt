using System;
using System.IO;
using System.Linq;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;
using ProjectBrain;
[Serializable] public class AbilityTemplateDomain {public string id;public string title;public string summary;}
[Serializable] public class AbilityTemplateScript {public string path;public string feature;public string role;public string intent;public string cautions;public string body;public string[] dependencies;}
[Serializable] public class AbilityTemplateFile {public int schemaVersion;public string name;public AbilityTemplateDomain[] domains;public AbilityTemplateDomain[] features;public AbilityTemplateScript[] scripts;}
public class ImportAbilityTemplate {
 public static object Main(){
  var root=ScriptDocumentService.ProjectRoot;
  var service=new ScriptDocumentService();
  service.LoadOrCreate(AssetDatabase.LoadAssetAtPath<MonoScript>("Assets/AbilityFramework/Runtime/AbilityClock.cs"));
  var template=new AbilityTemplateFile {schemaVersion=1,name="Unity Ability Kit",domains=new AbilityTemplateDomain[]{new AbilityTemplateDomain {id="domain:abilities",title="능력 시스템",summary="조건 확인과 실행 확정"},new AbilityTemplateDomain {id="domain:attributes",title="능력치",summary="기본값과 최종값의 계산"},new AbilityTemplateDomain {id="domain:effects",title="효과와 상태",summary="효과 수명·태그·시간"},new AbilityTemplateDomain {id="domain:unity",title="Unity 연결",summary="설정 에셋·캐릭터·예제 UI"},new AbilityTemplateDomain {id="domain:testing",title="자동 테스트",summary="실패·경계 조건 검증"}},features=new AbilityTemplateDomain[]{new AbilityTemplateDomain {id="feature:abilities/activation",title="발동 조건과 실행",summary="조건을 검사한 뒤 비용·쿨다운·효과를 확정"},new AbilityTemplateDomain {id="feature:attributes/values",title="능력치 계산",summary="기본값과 일시 modifier를 분리"},new AbilityTemplateDomain {id="feature:effects/lifetime",title="효과 수명",summary="즉시·지속 효과와 태그 회수"},new AbilityTemplateDomain {id="feature:unity/configuration",title="스킬 설정",summary="공유 ScriptableObject 정의"},new AbilityTemplateDomain {id="feature:unity/integration",title="캐릭터와 시연",summary="Unity 어댑터와 UI Toolkit 예제"},new AbilityTemplateDomain {id="feature:testing/contracts",title="실행 계약 검사",summary="NUnit으로 정상·거절·경계를 확인"}},scripts=new AbilityTemplateScript[]{new AbilityTemplateScript {path="Assets/AbilityFramework/Runtime/AbilityClock.cs",feature="feature:effects/lifetime",role="게임 시간과 테스트 시간을 분리한다.",intent="쿨다운과 효과 만료를 실제 대기 없이 재현하기 위해 시간을 주입한다.",cautions="시계는 음수·역행·NaN·무한대를 허용하지 않는다. Unity 연결은 scaled game time을 사용한다.",body="IAbilityClock.Now가 실행기의 시간 기준이다. ManualAbilityClock.Advance로 정확히 경계 시각을 검사한다. 게임에서는 AbilitySystemComponent가 Unity 시계를 주입한다.",dependencies=new string[]{}},new AbilityTemplateScript {path="Assets/AbilityFramework/Runtime/GameplayTags.cs",feature="feature:effects/lifetime",role="기절·가속 같은 상태를 계층 태그로 표현한다.",intent="문자열 비교가 흩어지는 것을 막고 효과 여러 개의 상태 소유를 참조 수로 관리한다.",cautions="대소문자를 구분한다. State.Stun은 State.Stunned와 다르다. 수동 Add/Remove를 쓰는 호출자는 짝을 맞춰야 한다.",body="State.Stunned는 State 조회와 일치한다. 중복 부여는 참조 수를 늘리고 효과 만료 시 해당 수만 줄인다. 모든 부여가 끝났을 때 태그가 사라진다.",dependencies=new string[]{}},new AbilityTemplateScript {path="Assets/AbilityFramework/Runtime/AttributeSet.cs",feature="feature:attributes/values",role="체력·마나·공격력·이동속도의 기본값과 일시 보정을 관리한다.",intent="버프가 끝날 때 과거 값을 덮어쓰지 않고 현재 기본값에서 다시 계산한다.",cautions="최종값=(기본값+가산 합)×곱셈 곱, 마지막에 범위 제한. 스킬 비용은 기본 자원값에서 소모한다. 일시 자원 보정은 비용에 포함하지 않는다.",body="Instant 효과는 기본값을 바꾼다. Duration 효과는 handle별 modifier를 추가한다. 예: 속도5에1.5배 버프→7.5, 중간 기본속도8→12, 만료→8. 값 경계와 유한 수를 검사한다.",dependencies=new string[]{}},new AbilityTemplateScript {path="Assets/AbilityFramework/Runtime/GameplayEffect.cs",feature="feature:effects/lifetime",role="즉시 효과와 지속 효과의 불변 설정을 정의한다.",intent="같은 효과 에셋을 여러 캐릭터가 안전하게 공유하도록 실행 상태를 정의에서 분리한다.",cautions="지속 효과는 독립 중첩이며 refresh/stack 정책 선택 기능은 없다. 최대64개 활성 효과. Instant 태그 부여는 거절한다.",body="즉시 피해/회복은 기본 능력치를 수정한다. 가속/기절은 만료 시각을 가진 handle로 적용하고 만료/조기 제거 시 modifier와 태그 참조를 회수한다. ActiveEffectView는 화면에 남은 시간만 전달한다.",dependencies=new string[]{"Assets/AbilityFramework/Runtime/AttributeSet.cs","Assets/AbilityFramework/Runtime/GameplayTags.cs"}},new AbilityTemplateScript {path="Assets/AbilityFramework/Runtime/GameplayAbility.cs",feature="feature:abilities/activation",role="스킬 비용·쿨다운·대상·태그 조건·효과를 정의한다.",intent="화염탄·회복처럼 다른 스킬이 같은 실행기를 쓰도록 데이터와 규칙을 분리한다.",cautions="동기 즉시 실행만 지원한다. 비동기 AbilityTask, 애니메이션 대기, 취소 가능한 캐스팅, 네트워크 예측은 없다.",body="GameplayAbilityDefinition은 공유 가능한 불변 설정이다. ActivationResult는 성공/실패 이유를, AbilityEvent는 실행 시도 기록을 담는다. Self와 Other 대상을 구분하며 효과와 태그 입력 배열을 복사한다.",dependencies=new string[]{"Assets/AbilityFramework/Runtime/GameplayEffect.cs","Assets/AbilityFramework/Runtime/GameplayTags.cs","Assets/AbilityFramework/Runtime/AttributeSet.cs"}},new AbilityTemplateScript {path="Assets/AbilityFramework/Runtime/AbilitySystem.cs",feature="feature:abilities/activation",role="캐릭터가 소유한 능력·자원·쿨다운·활성 효과를 실행한다.",intent="실패한 실행이 자원을 소모하지 않도록 먼저 전체 조건을 검사한 뒤 확정한다.",cautions="단일 게임 스레드 전제. 사전 조건 검사는 일반 실패의 부분 적용을 방지하며 OS 트랜잭션/네트워크 롤백을 보장하지 않는다. 결과 이력은 최근64건.",body="실행 순서: 부여된 능력 확인→대상/사망/필수·차단 태그→쿨다운→자원→모든 효과의 대상 능력치/용량 검사→비용·쿨다운 확정→효과 적용. 외부 콜백은 확정 중 실행하지 않는다. Tick에서 만료 handle을 제거한다.",dependencies=new string[]{"Assets/AbilityFramework/Runtime/GameplayAbility.cs","Assets/AbilityFramework/Runtime/GameplayEffect.cs","Assets/AbilityFramework/Runtime/AttributeSet.cs","Assets/AbilityFramework/Runtime/GameplayTags.cs","Assets/AbilityFramework/Runtime/AbilityClock.cs"}},new AbilityTemplateScript {path="Assets/AbilityFramework/Unity/GameplayEffectAsset.cs",feature="feature:unity/configuration",role="Inspector에서 편집할 수 있는 효과 설정 에셋이다.",intent="Unity 직렬화 데이터와 엔진에 의존하지 않는 불변 효과 정의를 연결한다.",cautions="Build 시 입력을 검증한다. 실행 중 에셋 변경을 기존 활성 효과에 자동 반영하지 않는다.",body="Create > Ability Kit > Gameplay Effect로 생성한다. 능력치·Add/Multiply·크기·지속시간·태그를 편집하고 Build로 복사된 런타임 정의를 만든다.",dependencies=new string[]{"Assets/AbilityFramework/Runtime/GameplayEffect.cs","Assets/AbilityFramework/Runtime/AttributeSet.cs"}},new AbilityTemplateScript {path="Assets/AbilityFramework/Unity/GameplayAbilityAsset.cs",feature="feature:unity/configuration",role="스킬의 비용·쿨다운·대상·효과 에셋을 설정한다.",intent="스킬별 실행 코드 추가 없이 화염탄/회복/가속/기절을 구성한다.",cautions="효과 참조 누락과 잘못된 비용/시간은 Build에서 거절한다. 변경은 실행기 재생성 후 적용한다.",body="Create > Ability Kit > Gameplay Ability로 생성한다. initialAbilities에 넣어 캐릭터에 부여한다. Hero와 Dummy는 같은 Stun 에셋을 공유하지만 실행 상태는 별개다.",dependencies=new string[]{"Assets/AbilityFramework/Runtime/GameplayAbility.cs","Assets/AbilityFramework/Runtime/AttributeSet.cs","Assets/AbilityFramework/Unity/GameplayEffectAsset.cs"}},new AbilityTemplateScript {path="Assets/AbilityFramework/Unity/AbilitySystemComponent.cs",feature="feature:unity/integration",role="Unity 캐릭터의 생명주기를 실행기에 연결한다.",intent="MonoBehaviour 의존을 어댑터에 두어 핵심 규칙을 Unity 없이 검사할 수 있게 한다.",cautions="RebuildSystem은 캐릭터의 자원·쿨다운·효과를 초기화한다. 저장/불러오기나 네트워크 동기화 기능은 아니다.",body="Awake에서 초기 능력치·공유 스킬 정의·UnityClock으로 개인 AbilitySystem을 생성한다. Update는 Tick만 호출한다. UI와 다른 게임 코드는 System을 통해 능력을 실행한다.",dependencies=new string[]{"Assets/AbilityFramework/Runtime/AbilitySystem.cs","Assets/AbilityFramework/Runtime/AttributeSet.cs","Assets/AbilityFramework/Runtime/AbilityClock.cs","Assets/AbilityFramework/Unity/GameplayAbilityAsset.cs"}},new AbilityTemplateScript {path="Assets/AbilityFramework/Unity/AbilityArenaDemo.cs",feature="feature:unity/integration",role="스킬 버튼·실행 결과·능력치·남은 효과를 보여주는 사용 예제다.",intent="프레임워크 규칙과 시연용 입력/화면/시각 효과를 분리한다.",cautions="시연용 캐릭터와 간단한 선 효과다. 완성된 전투 게임이나 제작된 아트라고 설명하지 않는다. 버튼은 실패 이유 시연을 위해 쿨다운 중에도 클릭할 수 있다.",body="Firebolt→Dummy 체력 감소, Heal→Hero 회복, Haste→이동속도/효과 시간, Stun hero→실행 거절과 비용 보존을 확인한다. Reset actors는 개인 상태만 재생성한다. UI Toolkit을 사용한다.",dependencies=new string[]{"Assets/AbilityFramework/Unity/AbilitySystemComponent.cs","Assets/AbilityFramework/Runtime/AbilitySystem.cs","Assets/AbilityFramework/Runtime/GameplayAbility.cs","Assets/AbilityFramework/Runtime/AttributeSet.cs"}},new AbilityTemplateScript {path="Assets/AbilityFramework/Tests/Editor/AbilitySystemTests.cs",feature="feature:testing/contracts",role="능력 실행과 효과 수명 규칙을 실제 NUnit으로 검사한다.",intent="설명한 보장이 실제 코드에서 유지되는지 성공·실패·경계 상황으로 확인한다.",cautions="테스트 코드의 존재는 통과 결과가 아니다. 실제 Test Runner 결과와 run ID를 별도 확인한다.",body="자원·쿨다운 확정, 거절 시 무소모, 두 캐릭터 상태 분리, 기절 만료, modifier 기본값 보존, 태그 참조 수, 중첩/용량 한도, 잘못된 입력, 정확한 시간 경계, 이력 제한을 검사한다.",dependencies=new string[]{"Assets/AbilityFramework/Runtime/AbilitySystem.cs","Assets/AbilityFramework/Runtime/GameplayAbility.cs","Assets/AbilityFramework/Runtime/GameplayEffect.cs","Assets/AbilityFramework/Runtime/AttributeSet.cs","Assets/AbilityFramework/Runtime/GameplayTags.cs","Assets/AbilityFramework/Runtime/AbilityClock.cs"}}}};
  if(template.schemaVersion!=1)throw new InvalidOperationException("Unsupported template");
  var paths=template.scripts.ToDictionary(s=>s.path,s=>AssetDatabase.AssetPathToGUID(s.path),StringComparer.Ordinal);
  foreach(var row in template.scripts)if(!row.path.StartsWith("Assets/AbilityFramework/",StringComparison.Ordinal)||paths[row.path].Length!=32||row.dependencies.Any(d=>!paths.ContainsKey(d)))throw new InvalidOperationException("Invalid template path");
  var j=new UnityBrainJson();var store=new BrainStore(Path.Combine(root,".projectbrain"),j);var now=DateTime.UtcNow.ToString("O");
  Action<BrainNode> Upsert=value=>{var old=store.LoadNode(value.id);if(old!=null){if(string.IsNullOrEmpty(value.body))value.body=old.body;value.updatedUtc=old.updatedUtc;if(j.Write(old)==j.Write(value))return;value.updatedUtc=now;}store.SaveNode(value);};
  foreach(var d in template.domains)Upsert(new BrainNode{id=d.id,type="Domain",title=d.title,summary=d.summary,updatedUtc=now});
  foreach(var f in template.features)Upsert(new BrainNode{id=f.id,type="Feature",title=f.title,summary=f.summary,updatedUtc=now});
  foreach(var s in template.scripts)Upsert(new BrainNode{id="asset:"+paths[s.path],type="Code",title=Path.GetFileName(s.path),summary=s.role,assetGuid=paths[s.path],lastKnownPath=s.path,tags=new[]{"ability-kit"},updatedUtc=now});
  var existingRelations=store.LoadRelations();
  var relations=existingRelations.Where(r=>r.source!="ability-template").ToList();
  Action<string,string,string> edge=(a,b,t)=>relations.Add(new BrainRelation{id="relation:"+BrainWorkspace.Hash("ability-template\n"+a+"\n"+t+"\n"+b),from=a,to=b,type=t,source="ability-template",createdUtc=now});
  foreach(var d in template.domains)edge("project:unity-ability-kit",d.id,"contains");
  foreach(var f in template.features)edge("domain:"+f.id.Substring(8).Split('/')[0],f.id,"contains");
  foreach(var s in template.scripts)edge(s.feature,"asset:"+paths[s.path],"implemented_by");
  foreach(var relation in relations){var existing=existingRelations.FirstOrDefault(e=>e.id==relation.id);if(existing!=null)relation.createdUtc=existing.createdUtc;}
  if(j.Write(new BrainRelationFile{relations=existingRelations.OrderBy(r=>r.id).ToArray()})!=j.Write(new BrainRelationFile{relations=relations.OrderBy(r=>r.id).ToArray()}))store.SaveRelations(relations);
  foreach(var s in template.scripts){
   var script=AssetDatabase.LoadAssetAtPath<MonoScript>(s.path);var d=service.LoadOrCreate(script);var version=service.Version(d);
   d.role=s.role;d.designIntent=s.intent;d.cautions=s.cautions;d.body=s.body;d.relatedScriptGuids=s.dependencies.Select(p=>paths[p]).ToArray();
   service.Save(d,version);
  }
  foreach(var w in Resources.FindObjectsOfTypeAll<BrainExplorerWindow>())w.CreateGUI();
  var graph=new BrainGraphService(store);
  return new {nodes=graph.Nodes.Count,relations=graph.Relations.Length,documents=template.scripts.Length,domains=template.domains.Length,paths=paths.Select(p=>new {path=p.Key,guid=p.Value}).ToArray()};
 }
}