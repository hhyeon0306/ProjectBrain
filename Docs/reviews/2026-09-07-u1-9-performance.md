# U1-9 구조도 성능 빠른 점검 · 2026-09-07

사용자 지시: 금방 해결할 수 없으면 보류하고 다음 지적을 우선한다. 코드 수정 없음.

- 현재 프로젝트 Unity 편집 모드, 구조도 창1/맵1, 저장35노드·63관계, 표시34노드.
- 단일 관측: 그래프 로드83.56ms, 분리된 맵 생성127.27ms. 초기 측정이라 JIT/캐시 영향을 포함하며 정상 프레임 비용이 아니다.
- 현재 표시 맵에서 UpdatePositions12회 평균0.725ms, Fit3회 평균1.557ms, 전체 라벨Measure12회 평균0.235ms. 측정 전 위치/확대/오프셋을 복원했다. 실제 포인터 이벤트·다음 프레임 레이아웃·GPU/벡터 테셀레이션 비용은 포함하지 않는다.
- 코드 후보: 문서 저장마다 CreateGUI로 데이터와 전체 그래프를 재생성한다. 초기 생성/도메인 변경은 SetLocal/SetQuery/SetScope/Fit를 중복 호출한다. 드래그는 글자 측정·충돌 검사·style 변경을 매 입력마다 수행하지만 이번 동기 측정만으로 심한 지연의 주원인이라고 단정할 수 없다.
- 선 그리기 Draw/Painter2D/Unity 패널 렌더링은 별도 프레임 프로파일링 미실행. 원인 미확정이므로 무리한 캐시/렌더러 교체 없이 보류.
